---
phase: 03-interactions
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - public/index.html
autonomous: true
requirements:
  - INT-01
  - INT-02
  - INT-03

must_haves:
  truths:
    - "Survoler une bulle affiche un tooltip qui suit le curseur avec nom, stack, date+branche, message commit, phase GSD"
    - "Cliquer une bulle déclenche un pulse scale(1→1.15→1, 300ms) et appelle GET /open?path=..."
    - "Saisir dans le champ filtre estompe les bulles non-matchées à opacity 0.15 en 200ms"
    - "Vider le champ ou appuyer Escape rétablit opacity 1 sur toutes les bulles"
    - "Le fond de la carte s'adapte à prefers-color-scheme dark (par défaut) et light"
  artifacts:
    - path: "public/index.html"
      provides: "CSS variables --bg/--surface/--border/--text-primary/--text-muted/--text-code/--accent-gsd/--shadow dans :root et @media light"
      contains: "prefers-color-scheme"
    - path: "public/index.html"
      provides: "div#tooltip avec classes tt-nom, tt-stack, tt-row, tt-dot, tt-date, tt-sep, tt-branche, tt-commit, tt-gsd, tt-gsd-dot, tt-gsd-label"
      contains: "tt-gsd-dot"
    - path: "public/index.html"
      provides: "div#recherche-overlay avec input#recherche-input"
      contains: "recherche-overlay"
    - path: "public/index.html"
      provides: "fonction pulserBulle() et event listener click sur noeuds"
      contains: "pulserBulle"
  key_links:
    - from: "mousemove event D3 node"
      to: "deplacerTooltip(event)"
      via: "OFFSET_X=+12, OFFSET_Y=-20, clampage bords viewport"
      pattern: "OFFSET_X"
    - from: "input#recherche-input event 'input'"
      to: "node.transition().attr('opacity', ...)"
      via: "comparaison case-insensitive sur d.nom"
      pattern: "normalize.*NFD"
    - from: "click event sur noeud D3"
      to: "fetch('/open?path=' + encodeURIComponent(d.chemin))"
      via: "pulserBulle(event.currentTarget)"
      pattern: "encodeURIComponent.*chemin"
---

<objective>
Transformer public/index.html en interface interactive complète : tooltip riche au survol (INT-01),
ouverture VS Code au clic avec feedback visuel (INT-02), filtrage temps réel par nom (INT-03), et
thème dark/light via prefers-color-scheme.

Purpose: C'est la tranche verticale finale — après ce plan, l'utilisateur peut explorer la carte
et agir sur les projets directement depuis l'interface (goal Phase 3).
Output: public/index.html avec les 3 interactions complètes et le design system Phase 3.
</objective>

<execution_context>
@/Users/laurent/Documents/CLAUDE_PROJETS/map_project/.planning/phases/03-interactions/03-CONTEXT.md
@/Users/laurent/Documents/CLAUDE_PROJETS/map_project/.planning/phases/03-interactions/03-UI-SPEC.md
</execution_context>

<context>
@/Users/laurent/Documents/CLAUDE_PROJETS/map_project/.planning/ROADMAP.md
@/Users/laurent/Documents/CLAUDE_PROJETS/map_project/.planning/phases/03-interactions/03-01-SUMMARY.md

<interfaces>
<!-- Contrats extraits de public/index.html Phase 2 — le plan modifie ce fichier -->

Fonctions existantes (NE PAS supprimer, étendre si nécessaire) :
- couleur(d) → string hex — couleur activité
- couleurGSD(d) → string hex — couleur phase GSD
- labelStack(d) → string emojis
- labelJours(d) → string date relative
- afficherTooltip(event, d) — REMPLACER par la nouvelle version UI-SPEC
- deplacerTooltip(event) — REMPLACER par la version avec OFFSET_X/OFFSET_Y
- masquerTooltip() — conserver, adapter pour retirer classe .visible
- esc(s) — conserver tel quel (anti-XSS)
- sauvegarderPositions(nodes) — ne pas toucher
- lancerSimulation(nodes) — étendre : ajouter listener 'click' sur noeuds
- mettreAJourVisuels() — étendre : même listener 'click' sur les nouveaux noeuds

ProjectRecord fourni par /api/projects (après plan 01) :
{
  id, nom, chemin, has_git, stacks, branche,
  jours_depuis_commit, date_relative, rayon, opacite,
  has_gsd, has_state, phase_gsd,
  last_commit   ← NOUVEAU (string | null)
}

COULEURS_GSD existant :
const COULEURS_GSD = {
  'in-progress': '#58a6ff', 'complete': '#3fb950',
  'not-started': '#6e7681', 'paused': '#d29922',
  'blocked': '#f85149', 'unknown': '#6e7681'
}

Labels GSD pour tooltip (UI-SPEC) :
const LABELS_GSD = {
  'in-progress': 'En cours', 'complete': 'Terminé',
  'not-started': 'Non démarré', 'paused': 'En pause',
  'blocked': 'Bloqué', 'unknown': 'GSD actif'
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tâche 1 : CSS variables thème + redesign tooltip + overlay recherche</name>
  <files>public/index.html</files>
  <read_first>public/index.html lignes 1-99 (bloc &lt;style&gt;) — lire en entier pour connaître les
  sélecteurs existants (#tooltip, .tt-nom, .tt-row, .tt-gsd, #legende, #dot-ws) avant de les
  modifier. Ne pas re-lire au-delà de la ligne 99 pour cette tâche.</read_first>
  <action>
Dans le bloc &lt;style&gt; de &lt;head&gt;, apporter ces modifications :

1. REMPLACER les couleurs hardcodées du body par des variables CSS. Ajouter en tête du &lt;style&gt; :

    :root {
      --bg:           #0d1117;
      --surface:      #161b22;
      --border:       #30363d;
      --text-primary: #e6edf3;
      --text-muted:   #8b949e;
      --text-code:    #a5d6ff;
      --accent-gsd:   #3fb950;
      --shadow:       rgba(0, 0, 0, 0.55);
    }
    @media (prefers-color-scheme: light) {
      :root {
        --bg:           #f6f8fa;
        --surface:      #ffffff;
        --border:       #d0d7de;
        --text-primary: #1f2328;
        --text-muted:   #656d76;
        --text-code:    #0969da;
        --accent-gsd:   #1a7f37;
        --shadow:       rgba(0, 0, 0, 0.12);
      }
    }

2. Dans `html, body` : remplacer `background: #0d1117` par `background: var(--bg)`.

3. REMPLACER entièrement le bloc `#tooltip { ... }` et les classes `.tt-*` par :

    #tooltip {
      position: fixed;
      z-index: 100;
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 10px 12px;
      max-width: 240px;
      pointer-events: none;
      box-shadow: 0 4px 20px var(--shadow);
      opacity: 0;
      transform: translateY(4px);
      transition: opacity 160ms ease, transform 160ms ease;
    }
    #tooltip.visible {
      opacity: 1;
      transform: translateY(0);
    }
    .tt-nom    { font-size: 13px; font-weight: 600; color: var(--text-primary); margin-bottom: 6px; line-height: 1.2; }
    .tt-stack  { font-size: 14px; margin-bottom: 4px; }
    .tt-row    { display: flex; align-items: center; gap: 6px; color: var(--text-muted); margin-top: 3px; font-size: 12px; line-height: 1.4; }
    .tt-dot    { width: 7px; height: 7px; border-radius: 50%; flex-shrink: 0; }
    .tt-sep    { color: var(--text-muted); }
    .tt-branche { font-family: ui-monospace, SFMono-Regular, 'SF Mono', Menlo, monospace; font-size: 11px; color: var(--text-code); }
    .tt-commit { color: var(--text-muted); font-size: 12px; }
    .tt-gsd    { display: flex; align-items: center; gap: 4px; margin-top: 6px; padding-top: 6px; border-top: 1px solid var(--border); font-size: 11px; font-weight: 500; }
    .tt-gsd-dot  { width: 7px; height: 7px; border-radius: 50%; flex-shrink: 0; }
    .tt-gsd-label { color: var(--text-primary); }

4. AJOUTER les blocs CSS pour le champ de recherche (après les règles tooltip) :

    #recherche-overlay {
      position: fixed; top: 16px; left: 16px; z-index: 200;
      display: flex; align-items: center; gap: 8px;
      background: var(--surface); border: 1px solid var(--border);
      border-radius: 8px; padding: 7px 12px;
      box-shadow: 0 2px 8px var(--shadow);
      transition: border-color 200ms ease, box-shadow 200ms ease;
    }
    #recherche-overlay:focus-within {
      border-color: #58a6ff;
      box-shadow: 0 0 0 3px rgba(88, 166, 255, 0.15), 0 2px 8px var(--shadow);
    }
    .loupe {
      font-size: 15px; color: var(--text-muted); line-height: 1; user-select: none;
    }
    #recherche-input {
      background: transparent; border: none; outline: none;
      color: var(--text-primary);
      font-family: system-ui, -apple-system, sans-serif;
      font-size: 13px; width: 160px; caret-color: #58a6ff;
    }
    #recherche-input::placeholder { color: var(--text-muted); }
    #recherche-input::-webkit-search-cancel-button { display: none; }

5. AJOUTER la règle CSS pour le pulse Safari (après les règles .noeud existantes) :

    .bulle {
      transform-box: fill-box;
      transform-origin: center;
    }

6. METTRE À JOUR #legende et #dot-ws pour utiliser var(--surface), var(--border), var(--text-muted)
   à la place des valeurs hex hardcodées (#161b22, #30363d, #8b949e). Conserver tous les autres
   styles de #legende inchangés.
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const html = fs.readFileSync('./public/index.html', 'utf8');
const checks = [
  ['prefers-color-scheme', '--bg'],
  ['#tooltip.visible', 'opacity: 1'],
  ['#recherche-overlay', 'position: fixed'],
  ['transform-box: fill-box', '.bulle'],
  ['--surface', 'var(--surface)'],
];
checks.forEach(([a, b]) => {
  const ok = html.includes(a) && html.includes(b);
  console.log((ok?'OK':'FAIL') + ' : ' + a);
  console.assert(ok, 'Manquant : ' + a + ' ou ' + b);
});
" 2>&1</automated>
  </verify>
  <done>
  Le bloc &lt;style&gt; contient :
  - :root avec les 8 variables CSS dark, et @media prefers-color-scheme light avec les 8 valeurs light
  - background: var(--bg) sur html/body
  - #tooltip avec transition opacity+transform (pas display:none)
  - Toutes les classes .tt-* avec var(--text-*) et var(--border)
  - #recherche-overlay, .loupe, #recherche-input CSS complets
  - .bulle { transform-box: fill-box; transform-origin: center; }
  </done>
</task>

<task type="auto">
  <name>Tâche 2 : HTML overlay recherche + JS tooltip/clic/filtre</name>
  <files>public/index.html</files>
  <read_first>public/index.html lignes 100-576 (HTML body + JS) — lire en entier pour connaître :
  la position du div#tooltip (ligne ~103), les fonctions afficherTooltip/deplacerTooltip/masquerTooltip
  (lignes ~226-258), les blocs lancerSimulation et mettreAJourVisuels (lignes ~367-487), et la
  fin du fichier pour savoir où insérer sans casser la structure.</read_first>
  <action>
MODIFICATIONS dans &lt;body&gt; et &lt;script&gt; :

--- HTML BODY ---

1. Après `&lt;div id="tooltip"&gt;&lt;/div&gt;`, ajouter :
    &lt;div id="recherche-overlay"&gt;
      &lt;span class="loupe" aria-hidden="true"&gt;⌕&lt;/span&gt;
      &lt;input id="recherche-input" type="search" placeholder="Filtrer les projets…"
             autocomplete="off" spellcheck="false" /&gt;
    &lt;/div&gt;

--- JS DANS &lt;script&gt; ---

2. REMPLACER la fonction afficherTooltip() par :

    const LABELS_GSD = {
      'in-progress': 'En cours', 'complete': 'Terminé',
      'not-started': 'Non démarré', 'paused': 'En pause',
      'blocked': 'Bloqué', 'unknown': 'GSD actif'
    };

    function afficherTooltip(event, d) {
      const c = couleur(d);
      const stacks = labelStack(d);
      const branche = d.has_git && d.branche ? d.branche : null;
      const gsdCouleur = couleurGSD(d);
      const gsdLabel = d.has_state ? (LABELS_GSD[d.phase_gsd] || 'GSD actif') : null;
      tooltip.innerHTML = [
        `&lt;div class="tt-nom"&gt;${esc(d.nom)}&lt;/div&gt;`,
        stacks ? `&lt;div class="tt-stack"&gt;${stacks}&lt;/div&gt;` : '',
        `&lt;div class="tt-row"&gt;`,
          `&lt;div class="tt-dot" style="background:${c}"&gt;&lt;/div&gt;`,
          `&lt;span class="tt-date"&gt;${esc(d.date_relative)}&lt;/span&gt;`,
          branche ? `&lt;span class="tt-sep"&gt;·&lt;/span&gt;&lt;code class="tt-branche"&gt;${esc(branche)}&lt;/code&gt;` : '',
        `&lt;/div&gt;`,
        d.last_commit ? `&lt;div class="tt-row tt-commit"&gt;${esc(d.last_commit)}&lt;/div&gt;` : '',
        gsdLabel ? `&lt;div class="tt-gsd"&gt;&lt;span class="tt-gsd-dot" style="background:${gsdCouleur}"&gt;&lt;/span&gt;&lt;span class="tt-gsd-label"&gt;${esc(gsdLabel)}&lt;/span&gt;&lt;/div&gt;` : '',
      ].join('');
      tooltip.classList.add('visible');
      deplacerTooltip(event);
    }

3. REMPLACER la fonction deplacerTooltip() par :

    const OFFSET_X = 12;
    const OFFSET_Y = 20;
    function deplacerTooltip(event) {
      const tw = tooltip.offsetWidth, th = tooltip.offsetHeight;
      let x = event.clientX + OFFSET_X;
      let y = event.clientY - OFFSET_Y - th;
      if (x + tw > window.innerWidth  - 8) x = event.clientX - tw - OFFSET_X;
      if (y < 8)                           y = event.clientY + 8;
      tooltip.style.left = x + 'px';
      tooltip.style.top  = y + 'px';
    }

4. REMPLACER masquerTooltip() par :

    function masquerTooltip() { tooltip.classList.remove('visible'); }

5. AJOUTER la fonction pulserBulle() après masquerTooltip() :

    function pulserBulle(noeudG) {
      d3.select(noeudG).select('.bulle')
        .transition().duration(120).attr('transform', 'scale(1.15)')
        .transition().duration(180).attr('transform', 'scale(1)');
    }

6. Dans lancerSimulation(), dans le bloc de déclaration `const noeuds = ...` et après le
   `.call(d3.drag()...)`, ajouter le listener click sur `noeuds` :

    noeuds.on('click', (event, d) => {
      pulserBulle(event.currentTarget);
      fetch('/open?path=' + encodeURIComponent(d.chemin)).catch(() => {});
    });

7. Dans mettreAJourVisuels(), dans le bloc `enter => { const g = creerNoeud(enter); ... return g; }`,
   après le bloc `.call(d3.drag()...)`, ajouter le même listener click :

    g.on('click', (event, d) => {
      pulserBulle(event.currentTarget);
      fetch('/open?path=' + encodeURIComponent(d.chemin)).catch(() => {});
    });

8. AJOUTER le bloc de filtrage APRÈS la définition de `function connecterWS()` mais AVANT l'appel
   `connecterWS()` en fin de script :

    /* ===== Filtrage (INT-03) ===== */
    (function() {
      const input = document.getElementById('recherche-input');
      if (!input) return;

      function normaliser(s) {
        return String(s || '').toLowerCase().normalize('NFD').replace(/\p{Diacritic}/gu, '');
      }

      function appliquerFiltre() {
        const terme = normaliser(input.value);
        viewport.selectAll('g.noeud')
          .transition().duration(200)
          .attr('opacity', d => (!terme || normaliser(d.nom).includes(terme)) ? 1 : 0.15);
      }

      input.addEventListener('input', appliquerFiltre);

      input.addEventListener('keydown', e => {
        if (e.key === 'Escape') {
          input.value = '';
          appliquerFiltre();
          input.blur();
        }
      });
    })();

CONTRAINTES à respecter absolument :
- Ne pas ajouter stopPropagation() sur les nœuds (pitfall drag D3 — CONTEXT.md)
- Ne pas relancer simulation.alphaTarget() dans appliquerFiltre()
- Le tooltip utilise classList.add/remove('visible') — pas display:none/block
- d.chemin (pas d.path) — c'est le nom du champ dans ProjectRecord (scanner.js)
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const html = fs.readFileSync('./public/index.html', 'utf8');
const checks = [
  'recherche-overlay',
  'recherche-input',
  'LABELS_GSD',
  'tt-gsd-dot',
  'last_commit',
  'OFFSET_X',
  'pulserBulle',
  'encodeURIComponent(d.chemin)',
  'normalize.*NFD',
  'appliquerFiltre',
  'Escape',
];
let ok = true;
checks.forEach(pat => {
  const re = new RegExp(pat);
  const found = re.test(html);
  console.log((found?'OK':'FAIL') + ' : ' + pat);
  if (!found) ok = false;
});
process.exit(ok ? 0 : 1);
" 2>&1</automated>
  </verify>
  <done>
  - div#recherche-overlay présent dans le HTML entre #tooltip et #legende
  - afficherTooltip() utilise d.last_commit et LABELS_GSD
  - deplacerTooltip() utilise OFFSET_X=12 et OFFSET_Y=20
  - pulserBulle() présente et appelée dans le listener click
  - fetch('/open?path=' + encodeURIComponent(d.chemin)) présent dans les deux blocs lancerSimulation et mettreAJourVisuels
  - appliquerFiltre() utilise normalize('NFD') et transition D3 opacity 0.15/1
  - Escape vide le champ et déclenche appliquerFiltre()
  - Aucun stopPropagation() ajouté sur les nœuds D3
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DOM → innerHTML tooltip | Données projet injectées dans le DOM via innerHTML |
| input utilisateur → filtre D3 | Terme de recherche appliqué comme regex/comparaison sur les nœuds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering / XSS | afficherTooltip() innerHTML | mitigate | Toutes les valeurs dynamiques (d.nom, d.branche, d.last_commit, gsdLabel) passent par esc() avant injection dans innerHTML — déjà présent depuis Phase 1. |
| T-03-05 | Tampering | appliquerFiltre — normalize() | accept | Données locales en lecture seule (noms de dossiers) — aucun accès réseau, aucun stockage des termes recherchés. |
| T-03-06 | Information Disclosure | fetch('/open?path=...') | accept | L'URL contient le chemin absolu dans les logs browser — outil local uniquement, pas d'exposition réseau externe. |
</threat_model>

<verification>
1. Ouvrir http://localhost:3000 — le fond canvas est sombre (dark) ou clair selon le thème système
2. Survoler une bulle — tooltip visible en haut à droite du curseur, contient nom (gros), emojis stack, date+branche en monospace, message commit (si présent), phase GSD avec couleur
3. Déplacer la souris sur la bulle — le tooltip suit le curseur
4. Sortir de la bulle — tooltip disparaît avec fade-out
5. Cliquer une bulle — pulse bref scale 1.15 visible (~300ms), VS Code s'ouvre sur le projet
6. Saisir "bot" dans le champ — les bulles sans "bot" dans leur nom s'estompent à 0.15 opacity
7. Appuyer Escape — toutes les bulles reviennent à opacity 1, champ vidé
8. Les positions sauvegardées en localStorage ne sont pas réinitialisées
9. Le drag des bulles fonctionne toujours après les modifications
</verification>

<success_criteria>
- INT-01 : tooltip visible au survol, suit le curseur, contient les 5 éléments (nom, stack, date+branche, commit, GSD)
- INT-02 : clic → pulse bulle + fetch /open → VS Code ouvre le projet
- INT-03 : filtrage temps réel par nom, opacity 0.15 non-matchés, Escape réinitialise
- Thème dark/light : fond canvas et tooltip s'adaptent à prefers-color-scheme
- Aucune régression : drag, zoom/pan, localStorage positions, WebSocket, anneaux GSD
</success_criteria>

<output>
Après complétion, créer `.planning/phases/03-interactions/03-02-SUMMARY.md` avec :
- Liste des modifications apportées à index.html (CSS sections, JS functions remplacées/ajoutées)
- Confirmation que les 3 requirements INT-01/02/03 sont vérifiés manuellement
- Toute décision d'implémentation prise durant l'exécution
</output>
