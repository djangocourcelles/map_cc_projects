---
phase: 03-interactions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scanner.js
  - server.js
autonomous: true
requirements:
  - INT-01
  - INT-02

must_haves:
  truths:
    - "GET /api/projects retourne chaque projet avec un champ last_commit (string tronqué à 60 chars ou null)"
    - "GET /open?path=/chemin/valide répond 204 et exécute 'code /chemin'"
    - "GET /open?path=/chemin/hors-workspace répond 400"
  artifacts:
    - path: "scanner.js"
      provides: "champ last_commit dans chaque ProjectRecord"
      contains: "git log -1 --format=%s"
    - path: "server.js"
      provides: "route GET /open avec validation chemin et exec code"
      contains: "startsWith('/open')"
  key_links:
    - from: "scanner.js::construireRecord()"
      to: "ProjectRecord.last_commit"
      via: "gitExec('git log -1 --format=%s', chemin)"
      pattern: "last_commit"
    - from: "server.js::route /open"
      to: "child_process.exec"
      via: "validation chemin workspace"
      pattern: "child_process.*exec.*code"
---

<objective>
Étendre le backend pour exposer les deux données manquantes de la Phase 3 : le message du
dernier commit dans chaque ProjectRecord (nécessaire pour le tooltip INT-01), et la route
HTTP GET /open qui ouvre un projet dans VS Code (INT-02).

Purpose: Le frontend de la Phase 3 (plan 02) consomme ces deux points d'extension. Sans eux,
ni le tooltip complet ni l'ouverture VS Code ne fonctionnent.
Output: scanner.js avec last_commit, server.js avec route /open opérationnelle.
</objective>

<execution_context>
@/Users/laurent/Documents/CLAUDE_PROJETS/map_project/.planning/phases/03-interactions/03-CONTEXT.md
</execution_context>

<context>
@/Users/laurent/Documents/CLAUDE_PROJETS/map_project/.planning/ROADMAP.md
@/Users/laurent/Documents/CLAUDE_PROJETS/map_project/.planning/phases/03-interactions/03-UI-SPEC.md

<interfaces>
<!-- Contrats existants à étendre — extraits de scanner.js et server.js -->

scanner.js — fonction construireRecord() retourne actuellement :
{
  id, nom, chemin,
  has_git, stacks, branche, jours_depuis_commit, date_relative,
  rayon, opacite, has_gsd, has_state, phase_gsd
}
NOUVEAU champ à ajouter : last_commit (string | null)

scanner.js — gitExec() :
function gitExec(commande, cwd) → string | null
Déjà utilisé : gitExec('git log -1 --format=%ct', chemin)
À appeler en plus : gitExec('git log -1 --format=%s', chemin)

server.js — pattern de route existant :
if (req.method === 'GET' && req.url === '/api/projects') { ... res.end(JSON.stringify(...)) }
Nouvelle route à ajouter AVANT le bloc fichiers statiques (qui contient urlNormalisee).

WORKSPACE constant dans scanner.js : '/Users/laurent/Documents/CLAUDE_PROJETS'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tâche 1 : Ajouter last_commit dans scanner.js</name>
  <files>scanner.js</files>
  <read_first>scanner.js — lire la fonction construireRecord() complète (lignes 109-147) pour connaître
  la position exacte où insérer les deux nouvelles lignes, et vérifier que gitExec est déjà importé.</read_first>
  <behavior>
    - Pour un projet avec git : last_commit = message du dernier commit tronqué à 60 chars, avec '…' si tronqué
    - Pour un projet sans git : last_commit = null
    - Pour un commit dont le message fait exactement 60 chars ou moins : pas de '…' ajouté
    - Le champ last_commit est présent dans l'objet retourné par construireRecord()
  </behavior>
  <action>
Dans construireRecord(), après la ligne `const branche = brancheRaw || ...` (ligne ~127), ajouter :

    const commitMsgRaw = hasGit ? gitExec('git log -1 --format=%s', chemin) : null;
    const last_commit = commitMsgRaw
      ? commitMsgRaw.slice(0, 60) + (commitMsgRaw.length > 60 ? '…' : '')
      : null;

Dans l'objet return (lignes ~132-146), ajouter après `phase_gsd: statutGSD,` :

    last_commit,

Ne pas modifier gitExec ni aucune autre fonction.
  </action>
  <verify>
    <automated>node -e "
const { scannerWorkspace } = require('./scanner');
const projets = scannerWorkspace();
const avecGit = projets.filter(p => p.has_git);
const sansGit = projets.filter(p => !p.has_git);
console.assert('last_commit' in projets[0], 'Champ last_commit absent');
if (avecGit.length > 0) {
  const lc = avecGit[0].last_commit;
  console.assert(lc === null || (typeof lc === 'string' && lc.length <= 61), 'last_commit trop long');
}
if (sansGit.length > 0) {
  console.assert(sansGit[0].last_commit === null, 'last_commit non-null sur projet sans git');
}
console.log('OK last_commit :', avecGit[0]?.last_commit ?? '(null)');
" 2>&1</automated>
  </verify>
  <done>Chaque ProjectRecord contient last_commit (string ≤ 61 chars avec '…' si tronqué, ou null si sans git).
  La commande de vérification imprime "OK last_commit :" sans assertion échouée.</done>
</task>

<task type="auto" tdd="true">
  <name>Tâche 2 : Ajouter route GET /open dans server.js</name>
  <files>server.js</files>
  <read_first>server.js — lire la fonction createServer (lignes 23-69) pour identifier l'emplacement
  exact où insérer la nouvelle route, après la route /api/projects (ligne ~40) et avant le bloc
  fichiers statiques (ligne ~43 : urlNormalisee).</read_first>
  <behavior>
    - GET /open?path=/Users/laurent/Documents/CLAUDE_PROJETS/MonProjet → 204, exec('code "..."')
    - GET /open?path=/etc/passwd → 400 "Chemin invalide"
    - GET /open sans paramètre path → 400 "Chemin invalide"
    - GET /open?path= (vide) → 400 "Chemin invalide"
    - Erreur exec (VS Code absent) → console.error côté serveur uniquement, client reçoit quand même 204
  </behavior>
  <action>
Dans le handler createServer, après le bloc `if (req.method === 'GET' && req.url === '/api/projects')` 
et son `return`, insérer le bloc suivant (avant la ligne `const urlNormalisee = ...`) :

    if (req.url.startsWith('/open') && req.method === 'GET') {
      const params = new URL(req.url, 'http://localhost').searchParams;
      const chemin = params.get('path') || '';
      if (!chemin.startsWith('/Users/laurent/Documents/CLAUDE_PROJETS/')) {
        res.writeHead(400); res.end('Chemin invalide'); return;
      }
      require('child_process').exec(`code "${chemin}"`, err => {
        if (err) console.error('[open] Erreur VS Code :', err.message);
      });
      res.writeHead(204); res.end();
      return;
    }

La constante WORKSPACE est définie dans scanner.js, pas dans server.js — utiliser la chaîne
littérale '/Users/laurent/Documents/CLAUDE_PROJETS/' directement dans server.js pour la validation
(per D-05, D-07 UI-SPEC.md).
  </action>
  <verify>
    <automated>node -e "
const http = require('http');
const { exec } = require('child_process');
// Démarrer serveur en mode test
process.env.PORT = 3099;
const server = require('./server');
" 2>&1 | head -5 || true

# Test réel via curl (serveur doit être arrêté avant)
node -e "
const http = require('http');
// Test unitaire de la logique de validation uniquement
const WORKSPACE_ROOT = '/Users/laurent/Documents/CLAUDE_PROJETS/';
const testCas = [
  { path: '/Users/laurent/Documents/CLAUDE_PROJETS/MonProjet', attendu: true },
  { path: '/etc/passwd', attendu: false },
  { path: '', attendu: false },
  { path: null, attendu: false },
];
testCas.forEach(t => {
  const chemin = t.path || '';
  const valide = chemin.startsWith(WORKSPACE_ROOT);
  console.assert(valide === t.attendu, 'Echec pour : ' + t.path);
  console.log((valide === t.attendu ? 'OK' : 'FAIL') + ' : ' + t.path);
});
" 2>&1</automated>
  </verify>
  <done>La route GET /open existe dans server.js. La validation accepte uniquement les chemins
  commençant par '/Users/laurent/Documents/CLAUDE_PROJETS/'. La réponse est 204 pour les chemins
  valides, 400 pour les autres.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client HTTP → route /open | Paramètre path fourni par le browser — potentiellement manipulé |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of Privilege | route /open, exec() | mitigate | Valider que chemin.startsWith('/Users/laurent/Documents/CLAUDE_PROJETS/') avant tout exec — rejette /etc/passwd, ../../../ etc. |
| T-03-02 | Tampering | exec(`code "${chemin}"`) | mitigate | Chemin entouré de guillemets doubles dans la commande exec — empêche l'injection d'arguments supplémentaires pour les chemins standard (espaces gérés). Risque résiduel faible : outil local uniquement. |
| T-03-03 | Information Disclosure | console.error message d'erreur | accept | Erreur visible uniquement dans le terminal local — pas d'exposition réseau (D-07). |
</threat_model>

<verification>
1. `node -e "const {scannerWorkspace} = require('./scanner'); const p = scannerWorkspace(); console.log('last_commit:', p[0]?.last_commit);"` — imprime une valeur non-undefined
2. Le champ `last_commit` apparaît dans la réponse JSON de `curl http://localhost:3000/api/projects | node -e "process.stdin.pipe(require('stream').Transform({transform(c,_,cb){console.log(JSON.parse(c)[0].last_commit);cb();}}))"` (serveur démarré)
3. `curl -s -o /dev/null -w "%{http_code}" "http://localhost:3000/open?path=/etc/passwd"` → 400
4. `curl -s -o /dev/null -w "%{http_code}" "http://localhost:3000/open?path=/Users/laurent/Documents/CLAUDE_PROJETS/Projet_BotAVV"` → 204
</verification>

<success_criteria>
- scanner.js : chaque ProjectRecord exposé par /api/projects contient last_commit (string ou null)
- server.js : GET /open répond 204 pour les chemins dans le workspace, 400 sinon
- Aucune régression sur GET /api/projects ni sur le service des fichiers statiques
</success_criteria>

<output>
Après complétion, créer `.planning/phases/03-interactions/03-01-SUMMARY.md` avec :
- Champs ajoutés à ProjectRecord (last_commit)
- Pattern exact de la route /open (pour que le plan 02 sache comment appeler fetch)
- Toute décision prise durant l'exécution (p. ex. valeur exacte de la constante de validation)
</output>
