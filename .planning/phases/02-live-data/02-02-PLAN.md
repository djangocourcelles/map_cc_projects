---
phase: 02-live-data
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - public/index.html
autonomous: true
requirements:
  - VIS-04
  - VIS-05
  - INT-04
must_haves:
  truths:
    - "La taille de chaque bulle correspond aux 5 paliers de rayon reçus du serveur (50/40/30/20/12 px)"
    - "Chaque bulle affiche un anneau coloré de 3px reflétant la phase GSD (in-progress→bleu, complete→vert, not-started→gris, paused→orange, blocked→rouge)"
    - "Les projets sans STATE.md ont un anneau gris tireté (stroke-dasharray SVG)"
    - "La carte se met à jour en moins de 3s sans rechargement manuel quand un fichier change"
    - "Un dot vert/orange/rouge en bas à droite indique l'état de la connexion WebSocket"
  artifacts:
    - path: "public/index.html"
      provides: "anneau SVG GSD, client WS avec backoff, dot WS, transitions D3 400ms"
      contains: "anneau-gsd"
  key_links:
    - from: "public/index.html"
      to: "ws://localhost:3000"
      via: "connecterWS() → new WebSocket()"
      pattern: "connecterWS"
    - from: "mettreAJourVisuels()"
      to: "simulation D3 existante"
      via: ".data(nodes, d => d.id).join() update branch uniquement"
      pattern: "mettreAJourVisuels"
---

<objective>
Transformer le frontend en tableau de bord vivant : encodage visuel par taille (paliers D-02), anneaux GSD colorés (D-04/D-05/D-06), client WebSocket avec backoff (D-11) et dot d'état (D-12). Chaque mise à jour serveur anime la carte via transitions D3 400ms (D-10).

Purpose: C'est la livraison visible de la Phase 2 — l'utilisateur voit enfin sa carte réagir en temps réel à l'activité de ses projets.
Output: `public/index.html` modifié — la Phase 1 reste fonctionnelle, les nouvelles capacités s'y greffent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-live-data/02-CONTEXT.md
@.planning/phases/02-live-data/02-RESEARCH.md
@.planning/phases/02-live-data/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Contrats du plan 02-01 que ce plan consomme. -->

Schema ProjectRecord Phase 2 (retourné par /api/projects et les messages WS) :
```json
{
  "id": "map_project",
  "nom": "map_project",
  "chemin": "/Users/laurent/Documents/CLAUDE_PROJETS/map_project",
  "has_git": true,
  "stacks": [{ "stack": "Node.js", "emoji": "🟢" }],
  "branche": "main",
  "jours_depuis_commit": 0,
  "date_relative": "aujourd'hui",
  "rayon": 50,
  "opacite": 1.0,
  "has_gsd": true,
  "phase_gsd": "in-progress",
  "has_state": true
}
```
Champs nouveaux Phase 2 : `rayon` (50/40/30/20/12), `opacite` (1.0 ou 0.4), `phase_gsd` (string|null), `has_state` (boolean).

Message WebSocket broadcast :
```json
{ "type": "mise_a_jour", "projets": [ /* ProjectRecord[] */ ] }
```

Palette GSD (D-04) — hex exacts à utiliser :
```javascript
const COULEURS_GSD = {
  'in-progress':  '#58a6ff',  // bleu GitHub
  'complete':     '#3fb950',  // vert
  'not-started':  '#6e7681',  // gris
  'paused':       '#d29922',  // orange/ambre
  'blocked':      '#f85149',  // rouge GitHub
  'unknown':      '#6e7681',  // fallback = not-started
};
const COULEUR_SANS_STATE = '#444d56';  // gris foncé tireté (D-05)
```

Fonctions clés existantes dans index.html (à conserver et étendre) :
```javascript
lancerSimulation(nodes)     // init unique — NE PAS appeler depuis le handler WS
sauvegarderPositions(nodes) // sauvegarde localStorage — appeler après chaque mise à jour
CLE_LS                      // 'map_cc_positions'
viewport                    // <g class="viewport"> D3 selection
```
</interfaces>

<tasks>

<task type="auto">
  <name>Tâche 1 : Ajouter anneau GSD SVG + adapter le rendu des bulles aux paliers</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (lire l'intégralité — localiser lancerSimulation, la fonction enter, les styles CSS existants)
    - .planning/phases/02-live-data/02-RESEARCH.md §Pattern 4 (update D3 sans recréer simulation), §Pattern 6 (anneau SVG), §Pitfall 6 (forceCollide recalibration)
    - .planning/phases/02-live-data/02-CONTEXT.md §D-04, D-05, D-06, D-10
  </read_first>
  <action>
Modifier `public/index.html` en plusieurs points, sans toucher à la logique de zoom/pan, localStorage, tooltip ou drag :

**1. Ajouter `couleurGSD(d)` dans le bloc `/* ===== Utilitaires ===== */` :**
```javascript
const COULEURS_GSD = {
  'in-progress': '#58a6ff',
  'complete':    '#3fb950',
  'not-started': '#6e7681',
  'paused':      '#d29922',
  'blocked':     '#f85149',
  'unknown':     '#6e7681',
};
const COULEUR_SANS_STATE = '#444d56';

function couleurGSD(d) {
  if (!d.has_state) return COULEUR_SANS_STATE;
  return COULEURS_GSD[d.phase_gsd] || COULEURS_GSD['unknown'];
}
```

**2. Dans la fonction `enter` de `lancerSimulation()`, après l'append du cercle `.bulle` et AVANT les textes, ajouter l'anneau GSD (D-06 : 3px fixe, anneau rendu après bulle pour être au-dessus) :**
```javascript
// Anneau GSD (VIS-05) — rendu après .bulle (z-order SVG)
g.append('circle')
  .attr('class', 'anneau-gsd')
  .attr('r', d => d.rayon + 3)
  .attr('fill', 'none')
  .attr('stroke-width', 3)
  .attr('stroke', d => couleurGSD(d))
  .attr('stroke-dasharray', d => d.has_state ? null : '5 4');
```

**3. Dans `lancerSimulation()`, le `rayon` vient désormais du serveur (paliers D-02). La fonction `couleur(d)` Phase 1 (vert/orange/gris selon jours) reste utilisée pour le `fill` de la bulle et les textes. Vérifier que `d.rayon` est bien utilisé (pas recalculé localement) dans :**
- `.attr('r', d => d.rayon)` sur `.bulle`
- `.attr('r', d => d.rayon + 3)` sur `.anneau-gsd`
- `.attr('r', d => ...)` sur `.halo`
- `d3.forceCollide(d => d.rayon + 6)`

**4. L'opacité vient maintenant du serveur (`d.opacite`). Modifier le `.bulle` pour utiliser `d.opacite` :**
```javascript
.attr('opacity', d => d.opacite)
```
Supprimer tout calcul d'opacité local résiduel côté frontend.

**5. Recalibrer `forceCollide` dans la simulation (Pitfall 6) :**
Les rayons vont de 12 à 50px. Vérifier que la ligne existante utilise `d => d.rayon + 6`. Si ce n'est pas le cas, corriger. Ajouter `.strength(0.9)` si absent.

**6. Mettre à jour la légende (bas gauche) pour refléter les paliers de taille et les couleurs GSD :**
Remplacer le contenu de `#legende` par deux sections :
- Section "Taille — activité" : 5 items (cercle de taille visuelle 50/40/30/20/12 et label jours)
- Section "Anneau — statut GSD" : 5 couleurs sémantiques + 1 tireté pour sans STATE.md

Exemple CSS pour les cercles de taille dans la légende :
```css
.leg-cercle { border-radius: 50%; background: #3fb950; display: inline-block; flex-shrink: 0; }
```
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const src = fs.readFileSync('./public/index.html', 'utf8');
const checks = [
  ['COULEURS_GSD',         src.includes('COULEURS_GSD')],
  ['couleurGSD function',  src.includes('function couleurGSD(d)')],
  ['anneau-gsd class',     src.includes('anneau-gsd')],
  ['stroke-width 3',       src.includes('stroke-width.*3') || src.includes('stroke-width\', 3')],
  ['stroke-dasharray',     src.includes('stroke-dasharray')],
  ['has_state',            src.includes('has_state')],
  ['has_state null',       src.includes('has_state ? null')],
  ['d.opacite',            src.includes('d.opacite')],
  ['in-progress hex',      src.includes('58a6ff')],
  ['complete hex',         src.includes('3fb950')],
  ['blocked hex',          src.includes('f85149')],
];
let ok = true;
checks.forEach(([label, result]) => {
  if (!result) { console.error('FAIL:', label); ok = false; }
  else console.log('OK:', label);
});
process.exit(ok ? 0 : 1);
" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `public/index.html` contient `class="anneau-gsd"` sur un `<circle>` append après `.bulle`
    - `stroke-width` de l'anneau est `3` (D-06 : valeur fixe)
    - `stroke-dasharray` vaut `null` si `d.has_state` est true, `'5 4'` sinon (D-05)
    - `COULEURS_GSD` contient les 5 clés : `in-progress` → `#58a6ff`, `complete` → `#3fb950`, `not-started` → `#6e7681`, `paused` → `#d29922`, `blocked` → `#f85149`
    - `d.opacite` est utilisé pour l'opacité de la bulle (pas de calcul local d'opacité résiduel)
    - `d.rayon` (valeur serveur) est utilisé pour `.attr('r', ...)` de `.bulle` et `.anneau-gsd`
    - La légende reflète les nouvelles sections taille et statut GSD
  </acceptance_criteria>
  <done>Chaque bulle affiche un anneau coloré de 3px représentant son statut GSD. Les projets sans STATE.md ont un anneau tireté gris foncé. La taille des bulles vient des paliers serveur.</done>
</task>

<task type="auto">
  <name>Tâche 2 : Client WebSocket avec backoff + dot d'état + fonction mettreAJourVisuels</name>
  <files>public/index.html</files>
  <read_first>
    - public/index.html (état après Tâche 1 — localiser la section fetch('/api/projects') et la fin du script)
    - .planning/phases/02-live-data/02-RESEARCH.md §Pattern 4 (mettreAJourVisuels sans recréer simulation), §Pattern 5 (backoff WS), §Pitfall 3 (positions écrasées), §Pitfall 4 (simulation rechargée)
    - .planning/phases/02-live-data/02-CONTEXT.md §D-10, D-11, D-12
  </read_first>
  <action>
Modifier `public/index.html` — uniquement la section `<script>` et le CSS/HTML pour le dot WS :

**1. Ajouter le dot WS dans le `<body>` avant `</body>` :**
```html
<div id="dot-ws" title="WebSocket"></div>
```

**2. Ajouter le CSS du dot dans `<style>` (D-12) :**
```css
#dot-ws {
  position: fixed;
  bottom: 12px;
  right: 12px;
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: #6e7681;
  transition: background 0.3s ease;
  z-index: 200;
}
```

**3. Ajouter `dotWS(etat)` dans le bloc utilitaires du script :**
```javascript
function dotWS(etat) {
  const el = document.getElementById('dot-ws');
  if (!el) return;
  const couleurs = { vert: '#3fb950', orange: '#d29922', rouge: '#f85149' };
  el.style.background = couleurs[etat] || '#6e7681';
}
```

**4. Ajouter `mettreAJourVisuels(nouvellesDonnees)` après `lancerSimulation` dans le script :**

Cette fonction met à jour les attributs visuels SANS relancer la simulation (Pitfall 4). Elle doit :
- Fusionner les positions localStorage dans les nouvelles données AVANT tout appel D3 (Pitfall 3)
- Ne pas modifier `fx`/`fy` (évite de perturber un drag en cours)
- Utiliser `.data(nodes, d => d.id).join()` avec la key function
- Dans la branche `update`, appliquer `.transition().duration(400)` (D-10) sur :
  - `.select('.bulle')` : `.attr('r', d => d.rayon)` et `.attr('opacity', d => d.opacite)`
  - `.select('.anneau-gsd')` : `.attr('r', d => d.rayon + 3)`, `.attr('stroke', d => couleurGSD(d))`, `.attr('stroke-dasharray', d => d.has_state ? null : '5 4')`
- Dans la branche `enter`, appeler la même logique de création de nœud que dans `lancerSimulation` (extraire une fonction `creerNoeud(enter)` partagée)
- Dans la branche `exit`, `.remove()`
- Après le join, relancer `simulation.force('collision', d3.forceCollide(d => d.rayon + 6).strength(0.9)).alpha(0.1).restart()` pour laisser la physique se réajuster (Pitfall 6)
- Sauvegarder les positions après mise à jour

Pour accéder à `simulation` depuis `mettreAJourVisuels`, déclarer `let simulation;` dans la portée du module (avant `lancerSimulation`), et dans `lancerSimulation` affecter `simulation = d3.forceSimulation(...)`.

**5. Ajouter le client WebSocket avec backoff (D-11) à la fin du script, après le bloc `fetch('/api/projects')` :**
```javascript
/* ===== Client WebSocket (INT-04) ===== */
let delaiReconnect = 1000;
const DELAI_MAX_WS = 30000;
let wsInstance = null;

function connecterWS() {
  // Éviter les doublons si reconnexion rapide
  if (wsInstance && wsInstance.readyState === WebSocket.CONNECTING) return;

  const ws = new WebSocket(`ws://localhost:${location.port || 3000}`);
  wsInstance = ws;

  ws.addEventListener('open', () => {
    delaiReconnect = 1000;  // reset backoff
    dotWS('vert');
  });

  ws.addEventListener('message', (evt) => {
    try {
      const msg = JSON.parse(evt.data);
      if (msg.type === 'mise_a_jour' && Array.isArray(msg.projets)) {
        mettreAJourVisuels(msg.projets);
      }
    } catch (e) {
      console.error('[ws] Message invalide :', e.message);
    }
  });

  ws.addEventListener('close', () => {
    wsInstance = null;
    if (delaiReconnect > DELAI_MAX_WS) {
      dotWS('rouge');
      return;  // arrêt (D-11 : plafond 30s)
    }
    dotWS('orange');
    setTimeout(() => {
      delaiReconnect = Math.min(delaiReconnect * 2, DELAI_MAX_WS);
      connecterWS();
    }, delaiReconnect);
  });

  ws.addEventListener('error', () => ws.close());
}

// Démarrer la connexion WS après le chargement initial
connecterWS();
```
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const src = fs.readFileSync('./public/index.html', 'utf8');
const checks = [
  ['dot-ws HTML',           src.includes('id=\"dot-ws\"')],
  ['dot-ws CSS fixed',      src.includes('position: fixed') && src.includes('bottom: 12px') && src.includes('right: 12px')],
  ['dotWS function',        src.includes('function dotWS(etat)')],
  ['mettreAJourVisuels',    src.includes('function mettreAJourVisuels(')],
  ['transition 400ms',      src.includes('duration(400)')],
  ['anneau transition',     src.includes('.select(\'.anneau-gsd\')')],
  ['connecterWS',           src.includes('function connecterWS()')],
  ['backoff 1000',          src.includes('delaiReconnect = 1000')],
  ['DELAI_MAX_WS 30000',    src.includes('30000')],
  ['ws open → vert',        src.includes('dotWS(\'vert\')')],
  ['ws close → orange',     src.includes('dotWS(\'orange\')')],
  ['ws arrêt → rouge',      src.includes('dotWS(\'rouge\')')],
  ['localStorage fusion',   src.includes('positionsSauvees') && src.includes('mettreAJourVisuels')],
];
let ok = true;
checks.forEach(([label, result]) => {
  if (!result) { console.error('FAIL:', label); ok = false; }
  else console.log('OK:', label);
});
process.exit(ok ? 0 : 1);
" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `public/index.html` contient `<div id="dot-ws"` dans le HTML body
    - `#dot-ws` CSS a `position: fixed`, `bottom: 12px`, `right: 12px` (D-12)
    - `function mettreAJourVisuels(nouvellesDonnees)` existe et contient `.transition().duration(400)` (D-10)
    - `.select('.anneau-gsd')` est dans la branche update avec `.transition().duration(400)`
    - `function connecterWS()` existe avec `delaiReconnect = 1000` et `DELAI_MAX_WS = 30000` (D-11)
    - `dotWS('vert')` appelé dans l'event `open`, `dotWS('orange')` dans `close`, `dotWS('rouge')` quand `delaiReconnect > DELAI_MAX_WS` (D-12)
    - La fusion `positionsSauvees` est faite dans `mettreAJourVisuels` AVANT toute opération D3 (Pitfall 3)
    - `simulation.force('collision', ...).alpha(0.1).restart()` est appelé après le join dans `mettreAJourVisuels` (Pitfall 6)
    - `connecterWS()` est appelé une fois à la fin du script (après le fetch initial)
  </acceptance_criteria>
  <done>La carte reçoit les mises à jour en temps réel via WebSocket. Les changements de taille et de couleur d'anneau sont animés 400ms. Le dot bas-droite indique l'état de la connexion. Le backoff exponentiel gère les déconnexions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Backend enrichi (02-01) + Frontend complet Phase 2 : anneaux GSD colorés, taille par paliers, client WebSocket avec backoff et dot d'état. Transitions D3 400ms sur chaque mise à jour.
  </what-built>
  <how-to-verify>
    1. Depuis le terminal dans map_project : `node server.js`
    2. Le browser s'ouvre sur http://localhost:3000 — vérifier le dot WS vert en bas à droite
    3. Observer les bulles : les projets actifs (0j ou <7j) doivent être nettement plus grosses (50 ou 40px) que les projets inactifs (12px)
    4. Observer les anneaux : le projet `map_project` (in-progress) doit avoir un anneau bleu `#58a6ff` ; les projets sans `.planning/STATE.md` ont un anneau gris tireté
    5. Ouvrir un second terminal et modifier un STATE.md :
       ```
       echo "" >> /Users/laurent/Documents/CLAUDE_PROJETS/map_project/.planning/STATE.md
       ```
    6. Dans le browser : la carte se met à jour en moins de 3 secondes sans rechargement. Observer l'animation de transition sur les bulles.
    7. Arrêter le serveur (Ctrl+C) : le dot passe à orange, puis rouge après épuisement du backoff
  </how-to-verify>
  <resume-signal>Taper "approuvé" si tout fonctionne, ou décrire les problèmes observés</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket server → client browser | Messages JSON reçus côté client depuis localhost |
| localStorage → D3 | Positions potentiellement malformées relues du storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | index.html — parse JSON WS | mitigate | `try/catch JSON.parse` + validation `msg.type === 'mise_a_jour'` + `Array.isArray(msg.projets)` avant toute opération D3 |
| T-02-06 | Denial of Service | index.html — backoff reconnect | accept | Arrêt après 30s (D-11) — évite la boucle infinie |
| T-02-07 | Information Disclosure | localStorage | accept | Positions pixel uniquement, pas de données sensibles |
</threat_model>

<verification>
Test complet Phase 2 :

```bash
# 1. Démarrer
node server.js
# Attendu : serveur démarré + "[watcher] Surveillance active"

# 2. Vérifier structure frontend
node -e "
const src = require('fs').readFileSync('./public/index.html', 'utf8');
['anneau-gsd', 'couleurGSD', 'mettreAJourVisuels', 'connecterWS',
 'dot-ws', 'dotWS', 'duration(400)', '58a6ff', 'f85149'].forEach(s => {
  console.log(s + ':', src.includes(s) ? 'OK' : 'MANQUANT');
});
"

# 3. Vérifier API enrichie
curl -s http://localhost:3000/api/projects | \
  node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));
  const p=d[0]; console.log('rayon:', p.rayon, '| phase_gsd:', p.phase_gsd, '| has_state:', p.has_state);"

# 4. Smoke test WebSocket — ouvrir browser, puis :
echo "" >> /Users/laurent/Documents/CLAUDE_PROJETS/map_project/.planning/STATE.md
# Observer la mise à jour dans le browser (≤ 3s)
```
</verification>

<success_criteria>
1. La carte affiche des bulles de 5 tailles distinctes selon l'activité — les projets récents sont visuellement dominants
2. Chaque bulle a un anneau de 3px de couleur sémantique correspondant à son statut GSD ; les projets sans STATE.md ont un anneau tireté gris
3. Modifier `.planning/STATE.md` déclenche une mise à jour animée (400ms) de la carte en moins de 3s
4. Le dot WebSocket est vert au démarrage, orange pendant la reconnexion, rouge si le serveur est indisponible
</success_criteria>

<output>
Après complétion, créer `.planning/phases/02-live-data/02-02-SUMMARY.md`
</output>
