---
phase: 02-live-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scanner.js
  - watcher.js
  - server.js
autonomous: true
requirements:
  - VIS-04
  - VIS-05
  - INT-04
must_haves:
  truths:
    - "GET /api/projects retourne des objets avec rayon conforme aux 5 paliers (0j→50, <7j→40, <30j→30, <90j→20, ≥90j→12)"
    - "GET /api/projects retourne phase_gsd (string | null) et has_state (boolean) pour chaque projet"
    - "Le serveur Node.js accepte des connexions WebSocket sur le même port que HTTP"
    - "Modifier .planning/STATE.md ou .git/COMMIT_EDITMSG dans le workspace déclenche un broadcast WS dans ≤ 3s"
  artifacts:
    - path: "watcher.js"
      provides: "chokidar ciblé + debounce 500ms + broadcast WebSocket"
      exports: ["demarrerWatcher"]
    - path: "scanner.js"
      provides: "calculerRayon() à 5 paliers, lireStatutGSD(), scan depth 2 sous-projets"
    - path: "server.js"
      provides: "WebSocketServer attaché au serveur HTTP, import de watcher.js"
  key_links:
    - from: "watcher.js"
      to: "scanner.js"
      via: "require('./scanner').scannerWorkspace()"
      pattern: "scannerWorkspace"
    - from: "server.js"
      to: "watcher.js"
      via: "demarrerWatcher(wss)"
      pattern: "demarrerWatcher"
---

<objective>
Étendre le backend pour exposer les données visuelles Phase 2 (taille par activité, statut GSD) et établir le pipeline WebSocket de bout en bout.

Purpose: Sans ce plan, le frontend n'a ni les données enrichies (`rayon` correct, `phase_gsd`, `has_state`) ni le canal temps réel pour les recevoir.
Output: `watcher.js` (nouveau), `scanner.js` et `server.js` modifiés. L'API et le WebSocket fonctionnent ; le frontend Phase 1 continue de tourner sans modification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-live-data/02-CONTEXT.md
@.planning/phases/02-live-data/02-RESEARCH.md
</context>

<interfaces>
<!-- Types et contrats extraits du code existant pour l'exécuteur. -->
<!-- Ne pas re-explorer le codebase — utiliser directement. -->

Depuis scanner.js (signatures actuelles à remplacer ou étendre) :
```javascript
// REMPLACER entièrement — formule linéaire incompatible avec D-02 :
function calculerRayon(jours) { /* 28 + (30 - jours) * 0.8 */ }

// REMPLACER — opacité n'est plus l'encodage principal (D-01) :
function calculerOpacite(jours, hasGit) { /* 0.4 / 1.0 / 0.6 / 0.3 */ }

// AJOUTER — lecture STATE.md :
function lireStatutGSD(cheminProjet) { /* retourne string | null */ }

// scannerWorkspace() retourne actuellement :
// { id, nom, chemin, has_git, stacks, branche, jours_depuis_commit,
//   date_relative, rayon, opacite, has_gsd }
// Phase 2 doit AJOUTER : phase_gsd (string|null), has_state (boolean)
// et MODIFIER : rayon (paliers D-02), opacite (D-01 : 1.0 sauf sans-git→0.4)
```

Depuis server.js (point d'extension ligne 9) :
```javascript
// Ligne 9 actuelle :
// WebSocket : Phase 2 (watcher.js)

// Après Phase 2, doit devenir :
const { WebSocketServer } = require('ws');
const { demarrerWatcher } = require('./watcher');
// + wss créé après http.createServer()
// + demarrerWatcher(wss) appelé après server.listen()
```

Pattern WebSocket (ws v8) :
```javascript
const wss = new WebSocketServer({ server }); // partage le port HTTP
wss.on('connection', (ws) => { ws.on('error', console.error); });
// broadcast depuis watcher.js :
wss.clients.forEach(client => {
  if (client.readyState === WebSocket.OPEN) client.send(payload);
});
```

Schema JSON du broadcast (INT-04) :
```json
{ "type": "mise_a_jour", "projets": [ /* ProjectRecord[] enrichi */ ] }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Tâche 1 : Étendre scanner.js — paliers D-02, statut GSD, sous-projets depth 2</name>
  <files>scanner.js</files>
  <read_first>
    - scanner.js (lire l'intégralité — remplacer calculerRayon et calculerOpacite, ajouter lireStatutGSD, étendre scannerWorkspace)
    - .planning/phases/02-live-data/02-CONTEXT.md §decisions (D-01 à D-03, D-13 à D-16)
    - .planning/phases/02-live-data/02-RESEARCH.md §Pitfall 5 (incompatibilité formule linéaire)
  </read_first>
  <action>
Modifier scanner.js en trois points :

**1. Remplacer `calculerRayon(jours)` entièrement (D-02) :**
```javascript
function calculerRayon(jours) {
  if (jours === null) return 12;   // sans git (D-03)
  if (jours === 0)    return 50;
  if (jours < 7)      return 40;
  if (jours < 30)     return 30;
  if (jours < 90)     return 20;
  return 12;
}
```

**2. Remplacer `calculerOpacite(jours, hasGit)` (D-01) :**
```javascript
function calculerOpacite(jours, hasGit) {
  if (!hasGit || jours === null) return 0.4;  // sans git uniquement (D-01, D-03)
  return 1.0;  // tous les projets avec git ont opacité 1.0
}
```

**3. Ajouter `lireStatutGSD(cheminProjet)` après `calculerOpacite` :**
```javascript
function lireStatutGSD(cheminProjet) {
  const stateFile = path.join(cheminProjet, '.planning', 'STATE.md');
  try {
    const contenu = fs.readFileSync(stateFile, 'utf8');
    const match = contenu.match(/^status:\s*(.+)$/m);
    return match ? match[1].trim() : 'unknown';
  } catch {
    return null;  // pas de STATE.md → anneau tireté (D-05)
  }
}
```

**4. Étendre `scannerWorkspace()` pour depth 2 (D-13 à D-16) et les nouveaux champs :**

Dans la fonction `scannerWorkspace()`, après la construction de l'objet retourné pour chaque projet racine, ajouter la même logique de scan pour les sous-dossiers (depth 2). Structure :

- Lire les entrées du répertoire du projet racine
- Pour chaque sous-dossier non exclu (DOSSIERS_EXCLUS) et non commençant par `.` :
  - Vérifier qu'il contient au moins un fichier sentinelle (SENTINELLES) — sinon l'ignorer (D-14)
  - Construire un ProjectRecord similaire avec `id` = `${nomParent}/${nomSousDossier}`, `nom` = `${nomParent}/${nomSousDossier}` (D-15)
  - DOSSIERS_EXCLUS s'applique aussi en depth 2 (D-16)

**5. Ajouter `phase_gsd` et `has_state` à chaque ProjectRecord (projet racine ET sous-projets) :**
```javascript
const statutGSD = lireStatutGSD(chemin);
// dans le return :
has_state: statutGSD !== null,
phase_gsd: statutGSD,
```

**6. Exporter `lireStatutGSD` en plus de `scannerWorkspace` :**
```javascript
module.exports = { scannerWorkspace, lireStatutGSD };
```
  </action>
  <verify>
    <automated>node -e "
const { scannerWorkspace } = require('./scanner');
// Test paliers D-02 via calculerRayon exposée indirectement
const { lireStatutGSD } = require('./scanner');

// Vérification paliers via un projet avec git
const projets = scannerWorkspace();
const avecGit = projets.filter(p => p.has_git);
if (avecGit.length === 0) { console.log('SKIP: aucun projet git'); process.exit(0); }
const r = avecGit[0].rayon;
if (![12, 20, 30, 40, 50].includes(r)) { console.error('FAIL: rayon', r, 'hors paliers'); process.exit(1); }

// Vérification champs phase_gsd et has_state présents
const p = projets[0];
if (!('phase_gsd' in p)) { console.error('FAIL: phase_gsd absent'); process.exit(1); }
if (!('has_state' in p)) { console.error('FAIL: has_state absent'); process.exit(1); }

// Vérification opacité sans git = 0.4
const sansGit = projets.filter(p => !p.has_git);
if (sansGit.length > 0 && sansGit[0].opacite !== 0.4) { console.error('FAIL: opacite sans git', sansGit[0].opacite); process.exit(1); }

console.log('OK — paliers, phase_gsd, has_state, opacite valides');
console.log('Projets totaux:', projets.length, '(dont sous-projets si sentinelle trouvée)');
" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `scanner.js` contient `function calculerRayon(jours)` avec les 5 paliers exacts : `return 12` (null), `return 50` (0j), `return 40` (<7j), `return 30` (<30j), `return 20` (<90j), `return 12` (≥90j)
    - `scanner.js` contient `function lireStatutGSD(cheminProjet)` avec regex `/^status:\s*(.+)$/m`
    - `scanner.js` contient `function calculerOpacite` qui retourne `0.4` uniquement si `!hasGit || jours === null`, sinon `1.0`
    - Chaque objet retourné par `scannerWorkspace()` possède les champs `phase_gsd` et `has_state`
    - `module.exports` inclut `lireStatutGSD`
    - La boucle de scan itère les sous-dossiers de chaque projet racine et exclut les entrées listées dans `DOSSIERS_EXCLUS`
    - `node -e "require('./scanner').scannerWorkspace()"` s'exécute sans erreur
  </acceptance_criteria>
  <done>scanner.js retourne des ProjectRecord enrichis avec rayon (5 paliers), opacite (D-01), phase_gsd (string|null), has_state (boolean). Les sous-projets avec sentinelle apparaissent labelisés parent/sous-projet.</done>
</task>

<task type="auto">
  <name>Tâche 2 : Créer watcher.js et brancher le WebSocket dans server.js</name>
  <files>watcher.js, server.js</files>
  <read_first>
    - server.js (lire l'intégralité — ajouter WebSocketServer et import watcher après ligne 9)
    - .planning/phases/02-live-data/02-RESEARCH.md §Pattern 1 (WebSocketServer), §Pattern 2 (watcher.js complet), §Pitfall 1 (EMFILE), §Pitfall 2 (regex ignored)
    - .planning/phases/02-live-data/02-CONTEXT.md §D-07, D-08, D-09, D-11
  </read_first>
  <action>
**1. Créer watcher.js (nouveau fichier) :**

```javascript
'use strict';

const chokidar   = require('chokidar');
const WebSocket  = require('ws');
const { scannerWorkspace } = require('./scanner');

const WORKSPACE = '/Users/laurent/Documents/CLAUDE_PROJETS';
const PATTERNS  = [
  `${WORKSPACE}/**/.planning/STATE.md`,
  `${WORKSPACE}/**/.git/COMMIT_EDITMSG`,
];

function demarrerWatcher(wss) {
  let timer = null;

  const watcher = chokidar.watch(PATTERNS, {
    ignoreInitial: true,
    persistent: true,
    // D-07 : lookahead négatif pour garder COMMIT_EDITMSG (Pitfall 2)
    ignored: /node_modules|\.git[/\\](?!COMMIT_EDITMSG)|venv|dist|__pycache__/,
    awaitWriteFinish: { stabilityThreshold: 300, pollInterval: 100 },
  });

  function rescanEtBroadcast() {
    let projets;
    try {
      projets = scannerWorkspace();  // D-08 : rescan total
    } catch (err) {
      console.error('[watcher] Erreur rescan :', err.message);
      return;
    }
    const payload = JSON.stringify({ type: 'mise_a_jour', projets });
    wss.clients.forEach(client => {
      if (client.readyState === WebSocket.OPEN) {
        client.send(payload);
      }
    });
  }

  // D-09 : debounce 500ms
  function declencherDebounce() {
    clearTimeout(timer);
    timer = setTimeout(rescanEtBroadcast, 500);
  }

  watcher
    .on('add',    declencherDebounce)
    .on('change', declencherDebounce)
    .on('unlink', declencherDebounce)
    .on('error',  (err) => console.error('[watcher] Erreur :', err.message));

  return watcher;
}

module.exports = { demarrerWatcher };
```

**2. Modifier server.js :**

Remplacer le commentaire ligne 9 `// WebSocket : Phase 2 (watcher.js)` par les imports :
```javascript
const { WebSocketServer } = require('ws');
const { demarrerWatcher } = require('./watcher');
```

Après `const server = http.createServer(...)` et AVANT `server.listen()`, ajouter :
```javascript
// WebSocket attaché au même port HTTP (INT-04)
const wss = new WebSocketServer({ server });
wss.on('connection', (ws) => {
  ws.on('error', (err) => console.error('[ws] Erreur client :', err.message));
});
```

Dans le callback de `server.listen()`, après les lignes existantes (console.log + exec open), ajouter :
```javascript
demarrerWatcher(wss);
console.log('[watcher] Surveillance active — STATE.md + COMMIT_EDITMSG');
```
  </action>
  <verify>
    <automated>node -e "
// Vérification structurelle de watcher.js
const fs = require('fs');
const src = fs.readFileSync('./watcher.js', 'utf8');
const checks = [
  ['demarrerWatcher', src.includes('function demarrerWatcher(wss)')],
  ['debounce 500',    src.includes('setTimeout(rescanEtBroadcast, 500)')],
  ['STATE.md pattern', src.includes('**/.planning/STATE.md')],
  ['COMMIT_EDITMSG',  src.includes('**/.git/COMMIT_EDITMSG')],
  ['ignored regex',   src.includes('COMMIT_EDITMSG')],
  ['readyState OPEN', src.includes('readyState === WebSocket.OPEN')],
  ['module.exports',  src.includes('module.exports = { demarrerWatcher }')],
];
const srv = fs.readFileSync('./server.js', 'utf8');
checks.push(['server imports ws', srv.includes('WebSocketServer')]);
checks.push(['server imports watcher', srv.includes('demarrerWatcher')]);
checks.push(['server crée wss', srv.includes('new WebSocketServer({ server })')]);
checks.push(['server démarre watcher', srv.includes('demarrerWatcher(wss)')]);
let ok = true;
checks.forEach(([label, result]) => {
  if (!result) { console.error('FAIL:', label); ok = false; }
  else console.log('OK:', label);
});
process.exit(ok ? 0 : 1);
" 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `watcher.js` existe et contient `function demarrerWatcher(wss)`
    - `watcher.js` contient `setTimeout(rescanEtBroadcast, 500)` (debounce 500ms, D-09)
    - `watcher.js` contient le pattern `**/.planning/STATE.md` et `**/.git/COMMIT_EDITMSG` (D-07)
    - `watcher.js` contient le regex `ignored` avec `(?!COMMIT_EDITMSG)` (lookahead négatif, Pitfall 2)
    - `watcher.js` vérifie `client.readyState === WebSocket.OPEN` avant `client.send()`
    - `server.js` contient `require('ws')` et `require('./watcher')`
    - `server.js` contient `new WebSocketServer({ server })` (partage du port HTTP)
    - `server.js` appelle `demarrerWatcher(wss)` à l'intérieur du callback de `server.listen()`
    - `node server.js` démarre sans erreur et imprime `[watcher] Surveillance active` (test manuel)
  </acceptance_criteria>
  <done>watcher.js opérationnel : chokidar surveille ciblé (D-07), debounce 500ms (D-09), broadcast JSON sur tous les clients WS ouverts (D-08). server.js expose le WebSocket sur le port 3000.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → scanner.js | Lecture de fichiers locaux non contrôlés (STATE.md dans des projets tiers) |
| watcher.js → wss.clients | Broadcast vers tous les clients WebSocket connectés sur localhost |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | scanner.js — lecture STATE.md | accept | Usage local uniquement, pas de données sensibles, lecture seule |
| T-02-02 | Information Disclosure | watcher.js — broadcast chemin projet | accept | localhost uniquement, chemins locaux non sensibles |
| T-02-03 | Denial of Service | scanner.js — depth 2 scan | mitigate | DOSSIERS_EXCLUS appliqué en depth 2 (D-16), scan synchrone limité à ~25 projets |
| T-02-04 | Elevation of Privilege | server.js — path traversal | accept | Déjà mitigé Phase 1 : `filePath.startsWith(PUBLIC_DIR)` vérification présente dans server.js |
</threat_model>

<verification>
Après exécution du plan 02-01 :

```bash
# 1. Lancer le serveur
node server.js
# Attendu : "Serveur démarré : http://localhost:3000" + "[watcher] Surveillance active"

# 2. Vérifier l'API enrichie
curl http://localhost:3000/api/projects | node -e "
const chunks = [];
process.stdin.on('data', c => chunks.push(c));
process.stdin.on('end', () => {
  const projets = JSON.parse(chunks.join(''));
  const p = projets[0];
  const champs = ['rayon', 'phase_gsd', 'has_state', 'opacite'];
  champs.forEach(c => console.log(c + ':', p[c]));
  const rayonsValides = [12, 20, 30, 40, 50];
  console.log('rayon valide:', rayonsValides.includes(p.rayon));
});
"

# 3. Test smoke WebSocket (ouvrir browser puis modifier un STATE.md)
echo "" >> /Users/laurent/Documents/CLAUDE_PROJETS/map_project/.planning/STATE.md
# Attendu dans les logs serveur : "[watcher] Erreur rescan" absent, broadcast envoyé
```
</verification>

<success_criteria>
1. `GET /api/projects` retourne des objets avec `rayon` dans `{12, 20, 30, 40, 50}`, `phase_gsd` (string|null), `has_state` (boolean), `opacite` = 1.0 sauf sans git → 0.4
2. `node server.js` imprime `[watcher] Surveillance active — STATE.md + COMMIT_EDITMSG`
3. Modifier `.planning/STATE.md` dans n'importe quel projet déclenche un broadcast WS dans ≤ 3s (vérification manuelle browser ouvert sur le plan 02-02)
</success_criteria>

<output>
Après complétion, créer `.planning/phases/02-live-data/02-01-SUMMARY.md`
</output>
